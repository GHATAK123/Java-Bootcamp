// All the instance member variable inside the interface are "public static final" by default.No need to write.
// We cannot define the function inside the interface because the function inside an interface is by default abstract one.
// we cannot create object of interface.
//  interface do not have constructor because all the members of interface of are static.
interface i1
{
    void someFunction();
}
class A implements i1 // interface implementation
{
public void someFunction()
{
    // abstract  function override
{

}
}